# Define the string
company_name = "hello"

# Use a set to store unique characters
unique_chars = set(company_name)

# Count the number of unique characters
num_unique_chars = len(unique_chars)

# Print the result
print("Number of unique characters:", num_unique_chars)
